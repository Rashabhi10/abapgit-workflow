projection;

define behavior for yrv_proj_travel_m alias TravelProcessor
use etag
{
  // scenario specific field control
  field ( mandatory ) BeginDate, EndDate, CustomerID;

  use create;
  use update;
  use delete;
  use action acceptTravel;
}